import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    flex: 1,
    alignItems: 'center',
  },
  text: {
    fontSize: 15,
    fontWeight: '500',
    marginTop: 10,
  },
  loading: {
    marginTop: 10,
  },
});

export { styles };
