import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  columnWrapper: {
    width: '90%',
    alignSelf: 'center',
    justifyContent: 'space-between',
  },
});

export { styles };
